// Generated by CoffeeScript 1.7.1
(function() {
  $(function() {
    var App, authString;
    authString = 'Basic ' + window.btoa('admin:a');
    $.ajax({
      type: 'POST',
      url: 'API/login',
      headers: {
        'Authorization': authString
      },
      async: false,
      success: function() {
        return console.log('Successfully logged in!');
      }
    });
    $.ajax({
      type: 'PUT',
      url: 'API/login',
      data: 'test',
      async: false,
      success: function() {
        return console.log('Successfully set the project!');
      }
    });
    App = Ember.Application.create();
    App.Router.map(function() {
      return this.resource('sessions', function() {
        return this.resource('session', {
          path: '/sessions/:session_uuid'
        });
      });
    });
    App.SessionsRoute = Ember.Route.extend({
      model: function() {
        return $.getJSON('API/planningpoker/planningpokersession');
      }
    });
    return App.SessionRoute = Ember.Route.extend({
      serialize: function(session) {
        return {
          session_uuid: session['uuid']
        };
      }
    });
  });

}).call(this);
